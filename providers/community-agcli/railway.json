{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && curl -sSfL https://github.com/unconst/agcli/releases/latest/download/agcli-linux-x86_64 -o /usr/local/bin/agcli && chmod +x /usr/local/bin/agcli"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
